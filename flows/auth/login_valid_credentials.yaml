appId: by.batteryfly.ev_charging.dev
---
# Regression test placeholder — not yet implemented
# ID: 7
# Scenario: Вход через email с правильным логином и правильным паролем
# Notes: Will call subflows/auth/login.yaml (reusable login subflow, currently a WIP
#   stub with only launchApp + one coordinate tap — needs finishing: email/password
#   input steps and a success assertion) and then assert successful login.

- launchApp
